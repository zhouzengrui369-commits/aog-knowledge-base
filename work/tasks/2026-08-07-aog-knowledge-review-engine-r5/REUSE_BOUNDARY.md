# R5 Reuse Boundary

`nashsu/llm_wiki` is used as an architectural reference only in R5.

No upstream GPL implementation file is copied, translated, ported line-for-line, or embedded into AOG. R5 implements independently specified review/browse requirements in the existing FastAPI/Next.js codebase.

This avoids making an implicit repository-wide licensing decision while still avoiding architectural reinvention.