# GOAL-AOG-ISSUE12-R4 — SUPERSEDED

Status: `SUPERSEDED_BY_OWNER_SCOPE_UPDATE`

The R4 source-safety work is retained as successor input, but its former exact-SHA deployment authorization is revoked.

Owner updated the project requirements before MiniMax local deployment:

- evaluate `nashsu/llm_wiki` for AOG knowledge-management reuse;
- make pending-review knowledge browsable rather than hidden;
- separate review visibility from VERIFIED operational/AI eligibility;
- prioritize a usable local acceptance path before Tencent Cloud public deployment.

The successor Goal is `GOAL-AOG-KNOWLEDGE-R5`. Do not use R4 as the active deployment contract.