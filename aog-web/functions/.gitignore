__pycache__/
*.pyc
*.pyo
.env
# 部署时由 build.sh 动态生成 (避免 50MB+ 进 git)
# 装 Linux cp311 x86_64 wheel: uv pip install --target vendor ... --python-version 3.11 --python-platform x86_64-unknown-linux-gnu
vendor/
# 部署时由 build.sh 动态 copy
aog_web/
